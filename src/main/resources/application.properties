spring.application.name=sysaba

server.servlet.context-path=/sysaba
server.port=${PORT:8080}

# Database properties
db.host=localhost
db.username=postgres
db.password=postgres
db.port=5432
db.database=dev

# Datasource
spring.datasource.url=jdbc:postgresql://${db.host}:${db.port}/${db.database}
spring.datasource.username=${db.username}
spring.datasource.password=${db.password}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

spring.main.banner-mode=off

rsa.private-key=classpath:certs/private-key.pem
rsa.public-key=classpath:certs/public-key.pem

# Configuração do Google OAuth2
spring.security.oauth2.client.registration.google.client-id=1047483535758-57btrdd1rrd3bl8f7qtg72vn7r9568c0.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/sysaba/login/oauth2/code/google
